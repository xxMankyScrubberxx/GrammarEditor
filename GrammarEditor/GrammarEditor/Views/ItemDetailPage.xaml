<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GrammarEditor.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ScrollView>
        <StackLayout Spacing="20" Padding="15">
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>
                <Label Text="Category" FontSize="Medium" Grid.Column="2" />
                <Entry Text="{Binding MSG_CAT}" FontSize="Small" Grid.Column="3"/>
                <Label Text="ID" FontSize="Medium" Grid.Column="0" />
                <Label Text="{Binding MSG_ID}" FontSize="Medium" Grid.Column="1" />
            </Grid>
            
            <Label Text="Message:" FontSize="Medium" />
            <Editor Text="{Binding MSG}" FontSize="Small"/>
            <Label Text="English:" FontSize="Medium" />
            <Editor Text="{Binding MSG_EN}" FontSize="Small" BackgroundColor="{Binding EN_STATUS}"/>
            <Label Text="Русский:" FontSize="Medium" />
            <Editor Text="{Binding MSG_RU}" FontSize="Small" BackgroundColor="{Binding RU_STATUS}"/>

            <Label Text="Notes" FontSize="Medium" />
            <Entry Text="{Binding NOTES}" FontSize="Small"/>
        </StackLayout>
    </ScrollView>
</ContentPage>